import React from "react";

export default function Header({generate}) {
  return (
    <div className="Header">
      <h1>News App</h1>
      {/* <button className="export" onClick={()=>generate()} >Export news</button> */}
    </div>
  );
}
